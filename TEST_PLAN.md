| Method/Behavior | Valid Case(s) | Exception / Invalid Case(s) | Boundary Case(s) | Oracle/Expected Result | Related JUnit Test(s) |
| --- | --- | --- | --- | --- | --- |
| VendingMachine() | Creates a new VendingMachine | N/A | N/A | State of the vending machine should be that all slots are empty, and the balance is 0 | testConstructorNoBalance(), testConstructorEmptySlots() |
| addItem(item, code) | Adds an item to empty slots A, B, C, or D | Invalid code, occupied slot | Empty -> Occupied slot, D is the last slot | Slot should be occupied by the given item, occupied or invalid slot should throw VendingMachineException | testAddItem(), testAddItemInvalidCode(), testAddItemOccupied() |
| getItem(code) | Retrieve the item in the occupied slot, retrieve from empty slot | Invalid code | Empty vs occupied slot, codes A-D | Item occupying the requested slot is returned, empty should return null, invalid code throws VendingMachineException | testGetItem(), testGetItemEmpty, testGetItemInvalidCode() |
| removeItem(code) | Remove existing item | Empty slot, invalid code | Occupied -> empty slot, codes A-D | Existing item should be returned/removed, empty or invalid slots should throw VendingMachineException | testRemoveItem(), testRemoveItemEmpty(), testRemoveItemInvalidCode() |
| insertMoney(amount) | Insert one or multiple positive amounts | Negative amount | Amount = 0, amount just above or below 0 | Amount should be >=0, balance should increase by inserted amount, negative amount should throw VendingMachineException | testInsertMoneyPositive(), testInsertMoneyMultiple(), testInsertMoneyZero(), testInsertMoneyNegative() |
| getBalance() | Checking initial balance, and checking after inserting money | N/A | Initial Balance = 0 | Returns balance w/o changing it | testGetBalanceNew(), testGetBalanceAfterInsertion() |
| makePurchase(code) | Purchase item with sufficient balance | Invalid code, empty slot, insufficient balance | Balance is exact price, just above, just below price | Returns true if successful purchase is made, returns false if insufficient balance, empty slot, or invalid code | testMakePurchase(), testMakePurchaseInsufficient(), testMakePurchaseInvalidCode(), testMakePurchaseEmpty() |
| returnChange() | Return a positive balance | N/A | Balance = 0 | Returns the current balance, resets balance to 0 | testReturnChange(), testReturnChangeZero() |
| VendingMachineItem(name, price) | Create item with a positive price | Negative price | Price = 0, slightly above or below 0 | Price must be >= 0, negative price throws VendingMachineException, given info should be stored | testVendingMachineItemConstructor(), testVendingMachineItemConstructorNegative(), testVendingMachineItemConstructorZero() |
| getName() | Retrieve stored item name | N/A | N/A | Should return supplied name | testGetName() |
| getPrice | Retrieve stored item price | N/A | Price = 0 | Should return supplied price | testGetPrice(), testGetPriceZero() |
| VendingMachineException() | Construct an exception | N/A | N/A | A VendingMachineException object should be created | testVendingMachineException() |
| VendingMachineException(reason) | Construct an exception with a message | N/A | Empty string | getMessage() should be able to get the reason | testVendingMachineExceptionMessage() |

The test that will be parameterized is addItemInvalidCode(), this has a very strong boundary which can allow for multiple meaningful tests along it, and all 6 tests should have the same result, causing an exception.